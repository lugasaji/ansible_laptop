return { 
    {
        'barrett-ruth/live-server.nvim',
        build = 'npm install -g live-server',
        cmd = { 'LiveServerStart', 'LiveServerStop' },
        config = true,
        args = {
          '--port={{ live_server_port }}',
	  '--browser={{ live_server_browser }}'
        }
    }
}
